# route53_register

Tiny Go CLI Program to register EC2 host in Route53

# usage

```
Usage of ./dns_update:
  -hostname string
        to use for registering the A record
  -zonename string
        to use for registering the A record
```
